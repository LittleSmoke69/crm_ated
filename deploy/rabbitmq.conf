# Limite absoluto dentro do cgroup (mem_limit 1g no compose).
# Evita alarm de memória quando o broker enxerga a RAM inteira da VPS.
vm_memory_high_watermark.absolute = 512MiB
disk_free_limit.absolute = 256MB
